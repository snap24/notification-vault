<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".NotiVaultApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.NotiVault">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ToastHistoryActivity"
            android:exported="false"
            android:label="Toast Logs" />

        <activity
            android:name=".util.AuthActionActivity"
            android:theme="@style/Theme.NotiVault.Transparent"
            android:exported="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true" />

        <activity
            android:name=".widget.WidgetFilterActivity"
            android:exported="false"
            android:label="Filter Feed" />

        <activity
            android:name=".AppRulesActivity"
            android:exported="false"
            android:label="App Capture Rules" />

        <activity
            android:name=".AppRuleEditActivity"
            android:exported="false"
            android:label="Edit Capture Rule"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".widget.WidgetConfigAuthActivity"
            android:exported="true"
            android:theme="@style/Theme.NotiVault.Transparent"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.NotiVaultService"
            android:exported="false"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="ongoing|silent" />
        </service>

        <service
            android:name=".service.BackupService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".service.ToastRecorderService"
            android:label="NotiVault Toast Recorder"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".service.NotiVaultTileService"
            android:label="NotiVault Recording"
            android:icon="@drawable/ic_lock"
            android:exported="true"
            android:enabled="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Option A: Quick Capture Widget -->
        <receiver
            android:name=".widget.CaptureWidgetProvider"
            android:exported="true"
            android:label="Quick Capture">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.zygisk_enc.notivault.widget.ACTION_TOGGLE_CAPTURE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_capture_info" />
        </receiver>

        <!-- Option B: Notification Feed Widget -->
        <receiver
            android:name=".widget.NotificationFeedWidgetProvider"
            android:exported="true"
            android:label="Recent Notifications">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.zygisk_enc.notivault.widget.ACTION_REFRESH_FEED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_notification_feed_info" />
        </receiver>

        <service
            android:name=".widget.NotificationFeedService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="true" />

        <!-- Option C: Vault Dashboard Widget -->
        <receiver
            android:name=".widget.VaultDashboardWidgetProvider"
            android:exported="true"
            android:label="Vault Dashboard">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.zygisk_enc.notivault.widget.ACTION_TOGGLE_CAPTURE_DASHBOARD" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_vault_dashboard_info" />
        </receiver>

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

    </application>

</manifest>
